{
  "scripts": {
    "install": "concurrently \"cd frontend && npm i\" \"cd backend && npm i\" ",
    "all": "concurrently \"cd frontend && npm run dev\" \"cd backend && npm run dev\" "
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
